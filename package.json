{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "scripts": {
    "release": "lerna publish",
    "build": "(cd packages/solana-adapter && yarn build) && (cd packages/react && yarn build)"
  },
  "workspaces": ["packages/*"],
  "publishConfig": {
    "access": "public"
  }
}
